language: emacs-lisp

sudo: false

env:
  matrix:
    - EMACS=emacs24
  global:
    - CASK=$HOME/.cask/bin/cask

addons:
  apt:
    sources:
      - cassou-emacs
    packages:
      - emacs24

before_install:
  - curl -fsSkL https://raw.github.com/cask/cask/master/go | python

script:
  - make compile test
